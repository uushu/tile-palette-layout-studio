# Layout Template Samples

These assets are optional visual-analysis hints. Import the sample from Unity Package Manager, assign a template to a TilePaletteProfile, and keep project-specific rules outside the package source.

Templates never bypass visual analysis or validation.
